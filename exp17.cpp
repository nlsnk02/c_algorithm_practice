#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int n;

int main () {
    scanf("%d", &n);
    int ml = n%5;

    string lst[n];
    for (;n--;cin >> lst[n]);
    sort (lst, lst+n);
    
    for



    return 0;
}